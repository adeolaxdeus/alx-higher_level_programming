-- This script creates a table in the current database in your My
-- The result a table with two column id and name.

-- SQL queries.
CREATE TABLE IF NOT EXISTS first_table ( id INT,
										 name VARCHAR(256)
										);
